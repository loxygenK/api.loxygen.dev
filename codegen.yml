overwrite: true
schema: "src/graphql/typedef/schema/**/*.graphql"
documents: null
generates:
  src/graphql/typedef/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  src/graphql/typedef/schema.graphql:
    plugins:
      - "schema-ast"
